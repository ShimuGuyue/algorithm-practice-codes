

```cpp
class Solution
{
public:
    void rotate(vector<int>& as, int k)
    {
        int n = static_cast<int>(as.size());
        vector<int> bs(n);
        for (int i{ 0 }; i < n; ++i)
        {
            bs[(i + k) % n] = as[i];
        }
        as = std::move(bs);
    }
};
```
