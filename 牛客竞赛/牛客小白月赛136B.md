

```cpp
void solve()
{
    int n, m, x, y, k;
    cin >> n >> m >> x >> y >> k;

    int ans = std::min(
        [n, m, x, y](int k)
        {
            int ans = 0;
            int cost = std::min(n / 3, k);
            ans += (n - cost) * x;
            k -= cost;
            cost = std::min(m / 4, k);
            ans += (m - cost) * y;
            k -= cost;
            return ans;
        }(k),
        [n, m, x, y](int k)
        {
            int ans = 0;
            int cost = std::min(m / 4, k);
            ans += (m - cost) * y;
            k -= cost;
            cost = std::min(n / 3, k);
            ans += (n - cost) * x;
            k -= cost;
            return ans;
        }(k)
    );
    std::cout << ans << '\n';
}
```

