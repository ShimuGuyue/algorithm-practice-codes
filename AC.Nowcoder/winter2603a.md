

```c++
void solve()
{
    int x;
    std::cin >> x;
    if (x == 2 || x == 6 || x == 12 || x == 20 || x == 30 || x == 42 || x == 56 || x == 72 || x == 90)
        std::cout << "YES\n";
    else
        std::cout << "NO\n";
}
```

