

```c++
void solve()
{
    int x, y;
    std::cin >> x >> y;
    if (x > y)
        std::cout << "Alice\n";
    else if (x == y)
        std::cout << "Draw\n";
    else
        std::cout << "Bob\n";
}
```

