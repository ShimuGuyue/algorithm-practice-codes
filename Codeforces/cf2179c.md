

```c++
void solve()
{
    int n;
    std::cin >> n;
    std::vector<int> as(n);
    for (auto& a : as)
    {
        std::cin >> a;
    }

    std::sort(as.begin(), as.end());

    int ans{ std::max(as[0], as[1] - as[0]) };
    std::cout << ans << '\n';
}
```

