

```cpp
void solve()
{
    int n;
    std::cin >> n;
    std::string s;
    std::cin >> s;

    int count0{ std::count(s.begin(), s.end(), '0') };
    if (count0 % 2 == 0)
        std::cout << "BOB\n";
    else
        if (count0 == 1)
            std::cout << "BOB\n";
        else
            std::cout << "ALICE\n";
}
```

