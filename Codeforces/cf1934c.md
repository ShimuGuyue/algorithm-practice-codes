

```cpp
void solve()
{
    int n, m;
    std::cin >> n >> m;

    int x, y;

    int a, b, c;
    std::cout << "? " << 1 << ' ' << 1 << std::endl;
    std::cin >> a;
    std::cout << "? " << 1 << ' ' << m << std::endl;
    std::cin >> b;

    x = (a + b - m + 3) / 2;
    y = -x + a + 2;

    if (x >= 1 && x <= n && y >= 1 && y <= m)
    {
        std::cout << "? " << x << ' ' << y << std::endl;
        std::cin >> c;
        if (c == 0)
        {
            std::cout << "! " << x << ' ' << y << std::endl;
            return;
        }
    }

    std::cout << "? " << n << ' ' << 1 << std::endl;
    std::cin >> c;

    x = (a - c + n + 1) / 2;
    y = -x + a + 2;
    std::cout << "! " << x << ' ' << y << std::endl;
}
```

