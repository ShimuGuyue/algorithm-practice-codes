

```c++
void solve()
{
    int n;
    std::cin >> n;

    std::cout << "Division ";
    if (n < 1400)
        std::cout << 4 << '\n';
    else if (n < 1600)
        std::cout << 3 << '\n';
    else if (n < 1900)
        std::cout << 2 << '\n';
    else
        std::cout << 1 << '\n';
}
```

