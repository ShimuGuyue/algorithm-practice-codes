

```c++
void solve()
{
    int l{ 1 }, r{ 1000000 };
    while (l < r)
    {
        int mid = (l + r + 1) / 2;
        std::cout << mid << std::endl;
        std::string op;
        std::cin >> op;
        if (op == "<")
        {
            r = mid - 1;
        }
        else
        {
            l = mid;
        }
    }
    std::cout << "! " << l << std::endl;
}
```

