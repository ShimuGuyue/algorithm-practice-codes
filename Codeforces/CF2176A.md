

```c++
void solve()
{
    int n;
    std::cin >> n;
    int max;
    std::cin >> max;
    int ans{};
    for (int i = 1; i < n; ++i)
    {
        int a;
        std::cin >> a;
        if (a < max)
            ++ans;
        else
            max = a;
    }
    std::cout << ans << '\n';
}
```

