

```c++
void solve()
{
    int n;
    std::cin >> n;

    if (n == 1)
    {
        std::cout << "FastestFinger\n";
        return;
    }

    if (n == 2)
    {
        std::cout << "Ashishgup\n";
        return;
    }

    if (n & 1)
    {
        std::cout << "Ashishgup\n";
        return;
    }

    if (std::popcount(uint32_t(n)) == 1)
    {
        std::cout << "FastestFinger\n";
        return;
    }

    int count_factor_2{ 0 };
    while (n % 2 == 0)
    {
        n /= 2;
        ++count_factor_2;
    }

    if (count_factor_2 >= 2)
    {
        std::cout << "Ashishgup\n";
        return;
    }

    if (is_prime(n))
        std::cout << "FastestFinger\n";
    else
        std::cout << "Ashishgup\n";
}
```

