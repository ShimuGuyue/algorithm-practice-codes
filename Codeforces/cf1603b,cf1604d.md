

```cpp
void solve()
{
    int x, y;
    std::cin >> x >> y;

    if (x > y)
    {
        std::cout << x + y << '\n';
    }
    else
    {
        int l{ y / x * x }, r{ y };
        int ans{ (l + r) / 2 };
        std::cout << ans << '\n';
    }
}
```

