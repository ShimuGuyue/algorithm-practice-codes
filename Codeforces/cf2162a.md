

```c++
void solve()
{
    int n;
    std::cin >> n;
    std::vector<int> as(n);
    for (auto &a : as)
    {
        std::cin >> a;
    }
    int ans{ *std::max_element(as.begin(), as.end()) };
    std::cout << ans << '\n';
}
```

