

```cpp
void solve()
{
    int n;
    std::cin >> n;
    std::vector<std::string> ss(n);
    for (auto& s : ss)
    {
        std::cin >> s;
    }

    for (auto& s : ss)
    {
        if (is_palindrome(s))
        {
            std::cout << "Yes\n";
            return;
        }
    }

    {
        std::set<std::string> t2;
        std::set<std::string> t3;
        for (auto &s : ss)
        {
            if (s.length() == 2)
            {
                std::string t(s.rbegin(), s.rend());
                if (t2.count(t))
                {
                    std::cout << "Yes\n";
                    return;
                }
                t2.insert(s);
            }
            else
            {
                std::string t(s.rbegin(), s.rend());
                if (t3.count(t))
                {
                    std::cout << "Yes\n";
                    return;
                }
                t3.insert(s);
            }
        }
    }

    {
        std::set<std::string> t2;
        for (int i{ 0 }; i < n; ++i)
        {
            std::string& s{ ss[i] };
            if (s.length() == 3)
            {
                std::string t;
                t += s[2];
                t += s[1];
                if (t2.count(t))
                {
                    std::cout << "Yes\n";
                    return;
                }
            }
            else
            {
                t2.insert(s);
            }
        }
    }
    {
        std::set<std::string> t2;
        for (int i{ n - 1 }; i >= 0; --i)
        {
            std::string& s{ ss[i] };
            if (s.length() == 3)
            {
                std::string t;
                t += s[1];
                t += s[0];
                if (t2.count(t))
                {
                    std::cout << "Yes\n";
                    return;
                }
            }
            else
            {
                t2.insert(s);
            }
        }
    }
    std::cout << "No\n";
}

bool is_palindrome(std::string& s)
{
    int n{ int(s.length()) };
    for (int i{ 0 }; i < n - 1 - i; ++i)
    {
        if (s[i] != s[n - 1 - i])
            return false;
    }
    return true;
}
```

