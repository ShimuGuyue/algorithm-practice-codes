

```c++
void solve()
{
    int n, m;
    std::string a, b, c;
    std::cin >> n;
    std::cin >> a;
    std::cin >> m;
    std::cin >> b;
    std::cin >> c;
    std::deque<char> ans(a.begin(), a.end());
    for (int i = 0; i < m; ++i)
    {
        if (c[i] == 'D')
            ans.push_back(b[i]);
        else
            ans.push_front(b[i]);
    }
    for (char c : ans)
    {
        std::cout << c;
    }
    std::cout << '\n';
}
```

