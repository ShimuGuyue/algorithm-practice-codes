

```c++
void solve()
{
    int n;
    std::cin >> n;
    std::string s;
    std::cin >> s;
    if (s.find("2026") != std::string::npos)
        std::cout << 0 << '\n';
    else
        if (s.find("2025") != std::string::npos)
            std::cout << 1 << '\n';
        else
            std::cout << 0 << '\n';
}
```

