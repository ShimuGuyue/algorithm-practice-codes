

```cpp
void solve()
{
    int x, y;
    std::cin >> x >> y;
    if (y > 0 && (x - y * 2) % 3 == 0 && x - y * 2 >= 0)
    {
        std::cout << "yes\n";
        return;
    }
    if (y == 0 && x % 3 == 0)
    {
        std::cout << "yes\n";
        return;
    }
    if (y < 0 && (x - y * -4) % 3 == 0 && x - y * -4 >= 0)
    {
        std::cout << "yes\n";
        return;
    }
    std::cout << "no\n";
}
```

