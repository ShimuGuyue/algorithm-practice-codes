

```c++
void solve()
{
    int n;
    std::cin >> n;
    std::string s;
    std::cin >> s;

    std::set<char> t;
    for (char c : s)
    {
        t.insert(c);
    }

    int ans{ n + t.size() };
    std::cout << ans << '\n';
}
```

