

```cpp
void solve()
{
    int n;
    std::cin >> n;
    std::vector<int> as(n);
    for (auto& a : as)
    {
        std::cin >> a;
    }
    for (auto a : as)
    {
        if (a == 100)
        {
            std::cout << "Yes\n";
            return;
        }
    }
    std::cout << "No\n";
}
```

