

```cpp
void solve()
{
    int n;
    std::cin >> n;
    std::string s;
    std::cin >> s;

    s.erase(std::unique(s.begin(), s.end()), s.end());
    if (s.size() == 2)
        std::cout << 2 << '\n';
    else
        std::cout << 1 << '\n';
}
```

