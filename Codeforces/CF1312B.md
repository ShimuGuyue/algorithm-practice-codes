

```cpp
void solve()
{
    int n;
    std::cin >> n;
    std::vector<int> as(n);
    for (auto& a : as)
    {
        std::cin >> a;
    }

    std::sort(as.rbegin(), as.rend());
    for (auto a : as)
    {
        std::cout << a << ' ';
    }
    std::cout << '\n';
}
```

