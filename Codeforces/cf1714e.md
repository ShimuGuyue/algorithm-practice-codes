

```cpp
void solve()
{
    int n;
    std::cin >> n;
    std::vector<int> as(n);
    for (auto& a : as)
    {
        std::cin >> a;
        if (a & 1)
            a += a % 10;
    }

    bool find0{ false };
    for (auto a : as)
    {
        if (a % 10 == 0)
            find0 = true;
    }
    if (find0)
    {
        for (int i{ 1 }; i < n; ++i)
        {
            if (as[i] != as[i - 1])
            {
                std::cout << "No\n";
                return;
            }
        }
        std::cout << "Yes\n";
        return;
    }

    for (auto& a : as)
    {
        while (a % 10 != 2)
        {
            a += a % 10;
        }
        a %= 20;
    }
    for (int i{ 1 }; i < n; ++i)
    {
        if (as[i] != as[i - 1])
        {
            std::cout << "No\n";
            return;
        }
    }
    std::cout << "Yes\n";
}
```

