

```cpp
void solve()
{
    std::string s;
    std::cin >> s;
    int count(std::count(s.begin(), s.end(), 'N'));
    if (count == 1)
        std::cout << "NO\n";
    else
        std::cout << "YES\n";
}
```

