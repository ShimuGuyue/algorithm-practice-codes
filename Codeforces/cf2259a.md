

```cpp
void solve()
{
    int n, k;
    cin >> n >> k;
    string s;
    cin >> s;

    int ans{ 0 };
    for (int i{ 0 }; i < n; i += k)
    {
        ans += std::find(s.begin() + i, s.begin() + i + k, '0') == s.begin() + i + k;
    }
    cout << ans << '\n';
}
```

