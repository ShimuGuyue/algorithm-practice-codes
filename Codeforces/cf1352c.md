

```c++
void solve()
{
    int n, k;
    std::cin >> n >> k;

    int ans{ k / (n - 1) * n };
    k %= n - 1;
    if (k == 0)
        --ans;
    else
        ans += k;
    std::cout << ans << '\n';
}
```

