

```c++
void solve()
{
    int n;
    std::cin >> n;
    std::vector<std::string> ss(n);
    for (auto &s : ss)
    {
        std::cin >> s;
    }

    std::string ans;
    for (auto &s : ss)
    {
        ans = std::min(s + ans, ans + s);
    }
    std::cout << ans << '\n';
}
```

