

```cpp
void solve()
{
    int n, a, b, k;
    std::cin >> n >> a >> b >> k;

    if (n == 2 || n == 3)
    {
        std::cout << 1 << '\n';
        return;
    }

    int dist{ std::abs(a - b) };
    dist = std::min(dist, n - dist);

    int ans{ dist + k };
    std::cout << ans << '\n';
}
```

