

```cpp
void solve()
{
    std::string s;
    std::cin >> s;

    int ans{ int(s.length()) - std::count(s.begin(), s.end(), '0') };

    [&s, &ans]
    {
        bool flag1{ false }, flag2{ false };
        int sum{ 0 };
        for (int i{ int(s.length() - 1) }; i >= 0; --i)
        {
            if (!flag1)
            {
                if (s[i] == '0')
                    flag1 = true;
                else
                    ++sum;
            }
            else if (!flag2)
            {
                if (s[i] == '0')
                    flag2 = true;
                else
                    ++sum;
            }
            else
            {
                break;
            }
        }
        if (flag1 && flag2)
            ans = std::min(ans, sum);
    }();
    [&s, &ans]
    {
        bool flag1{ false }, flag2{ false };
        int sum{ 0 };
        for (int i{ int(s.length()) - 1 }; i >= 0; --i)
        {
            if (!flag1)
            {
                if (s[i] == '5')
                    flag1 = true;
                else
                    ++sum;
            }
            else if (!flag2)
            {
                if (s[i] == '2')
                    flag2 = true;
                else
                    ++sum;
            }
            else
            {
                break;
            }
        }
        if (flag1 && flag2)
            ans = std::min(ans, sum);
    }();
    [&s, &ans]
    {
        bool flag1{ false }, flag2{ false };
        int sum{ 0 };
        for (int i{ int(s.length()) - 1 }; i >= 0; --i)
        {
            if (!flag1)
            {
                if (s[i] == '5')
                    flag1 = true;
                else
                    ++sum;
            }
            else if (!flag2)
            {
                if (s[i] == '7')
                    flag2 = true;
                else
                    ++sum;
            }
            else
            {
                break;
            }
        }
        if (flag1 && flag2)
            ans = std::min(ans, sum);
    }();
    [&s, &ans]
    {
        bool flag1{ false }, flag2{ false };
        int sum{ 0 };
        for (int i{ int(s.length()) - 1 }; i >= 0; --i)
        {
            if (!flag1)
            {
                if (s[i] == '0')
                    flag1 = true;
                else
                    ++sum;
            }
            else if (!flag2)
            {
                if (s[i] == '5')
                    flag2 = true;
                else
                    ++sum;
            }
            else
            {
                break;
            }
        }
        if (flag1 && flag2)
            ans = std::min(ans, sum);
    }();
    std::cout << ans << '\n';
}
```

