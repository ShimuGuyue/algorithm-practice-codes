

```c++
void solve()
{
    int n;
    std::cin >> n;
    std::deque<char> cs(n);
    for (auto& c : cs)
    {
        std::cin >> c;
    }
    while (!cs.empty() && cs.front() != cs.back())
    {
        cs.pop_front();
        cs.pop_back();
    }
    std::cout << cs.size() << '\n';
}
```

