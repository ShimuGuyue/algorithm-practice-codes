

```cpp
void solve()
{
    int n, k;
    std::cin >> n >> k;

    std::vector<int> as(n + 1);
    int o12, o13, o23;
    int a12, a13, a23;
    std::cout << "or " << 1 << ' ' << 2 << std::endl;
    std::cin >> o12;
    std::cout << "or " << 1 << ' ' << 3 << std::endl;
    std::cin >> o13;
    std::cout << "or " << 2 << ' ' << 3 << std::endl;
    std::cin >> o23;
    std::cout << "and " << 1 << ' ' << 2 << std::endl;
    std::cin >> a12;
    std::cout << "and " << 1 << ' ' << 3 << std::endl;
    std::cin >> a13;
    std::cout << "and " << 2 << ' ' << 3 << std::endl;
    std::cin >> a23;

    int x{ o12 + a12 };
    int y{ o13 + a13 };
    int z{ o23 + a23 };
    as[1] = (x + y - z) / 2;
    as[2] = (x - y + z) / 2;
    as[3] = (-x + y + z) / 2;
    for (int i{ 4 }; i <= n; ++i)
    {
        int a, o;
        std::cout << "or " << 1 << ' ' << i << std::endl;
        std::cin >> o;
        std::cout << "and " << 1 << ' ' << i << std::endl;
        std::cin >> a;
        as[i] = a + o - as[1];
    }

    std::sort(as.begin(), as.end());
    std::cout << "finish " << as[k] << std::endl;
}
```

