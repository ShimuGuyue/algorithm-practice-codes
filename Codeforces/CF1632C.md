

```c++
void solve()
{
    int a, b;
    std::cin >> a >> b;

    int ans{ INT_MAX };

    {
        int x{ a }, y{ b };
        int sum{ 0 };
        while ((x | y) != y)
        {
            ++sum;
            ++x;
        }
        if (x != y)
            ++sum;
        ans = std::min(ans, sum);
    }
    {
        int x{ a }, y{ b };
        int sum{ 0 };
        while ((x | y) != y)
        {
            ++sum;
            ++y;
        }
        if (x != y)
            ++sum;
        ans = std::min(ans, sum);
    }
    std::cout << ans << '\n';
}
```

