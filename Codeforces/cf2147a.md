

```c++
void solve()
{
    int x, y;
    std::cin >> x >> y;
    if (y > x)
        std::cout << 2 << '\n';
    else
        if (y != 1 && x - 1 > y)
            std::cout << 3 << '\n';
        else
            std::cout << -1 << '\n';
}
```

