

```c++
void solve()
{
    int n;
    std::cin >> n;
    std::map<int, int> m;
    while (n--)
    {
        int a;
        std::cin >> a;
        ++m[a];
    }
    std::cout << m.size() << '\n';
}
```

