

```cpp
void solve()
{
    int n, m, k;
    std::cin >> n >> m >> k;

    std::cout << ((m - 1) + (n - 1)) + ((m - 1) * n + (n - 1)) << '\n';
    std::cout << std::string(m - 1, 'L');
    std::cout << std::string(n - 1, 'U');
    int x{ 1 };
    for (int i{ 1 }; i <= n; ++i)
    {
        std::cout << std::string(m - 1, x == 1 ? 'R' : 'L');
        x = x == 1 ? n : 1;
        if (i != n)
            std::cout << 'D';
    }
    std::cout << '\n';
}
```

