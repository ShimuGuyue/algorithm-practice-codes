

```c++
void solve()
{
    int n;
    std::cin >> n;
    std::vector<int> as(n);
    for (auto& a : as)
    {
        std::cin >> a;
    }

    std::cout << (as.front() < as.back() ? "YES" : "NO") << '\n';
}
```

