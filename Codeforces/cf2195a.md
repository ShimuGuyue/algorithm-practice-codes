

```c++
void solve()
{
    int n;
    std::cin >> n;
    bool b{ false };
    while (n--)
    {
        int a;
        std::cin >> a;
        if (a == 67)
            b = true;
    }
    if (b)
        std::cout << "YES\n";
    else
        std::cout << "NO\n";
}
```

