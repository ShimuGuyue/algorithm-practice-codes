

```cpp
void solve()
{
    std::string s;
    std::cin >> s;

    static std::string t{ "codeforces" };

    int ans{ 0 };
    for (int i{ 0 }; i < 10; ++i)
    {
        ans += s[i] != t[i];
    }
    std::cout << ans << '\n';
}
```

