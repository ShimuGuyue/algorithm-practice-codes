

```c++
void solve()
{
    int a, b;
    std::cin >> a >> b;

    int flag;
    if ((a - 1) % 4 == 1)
        flag = 1;
    if ((a - 1) % 4 == 2)
        flag = (a - 1) + 1;
    if ((a - 1) % 4 == 3)
        flag = 0;
    if ((a - 1) % 4 == 0)
        flag = (a - 1);

    flag ^= b;
    if (flag == 0)
        std::cout << a << '\n';
    else if (flag == a)
        std::cout << a + 2 << '\n';
    else
        std::cout << a + 1 << '\n';
}
```

