

```c++
void solve()
{
    int n;
    std::cin >> n;
    std::vector<int> as(n);
    for (auto& a : as)
    {
        std::cin >> a;
    }

    for (int i = 2; i < n; i += 2)
    {
        if (as[i] % 2 != as[i - 2] % 2)
        {
            std::cout << "no\n";
            return;
        }
    }
    for (int i = 3; i < n; i += 2)
    {
        if (as[i] % 2 != as[i - 2] % 2)
        {
            std::cout << "no\n";
            return;
        }
    }
    std::cout << "yes\n";
}
```

