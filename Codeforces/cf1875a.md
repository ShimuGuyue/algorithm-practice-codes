

```cpp
void solve()
{
    int a, b, n;
    std::cin >> a >> b >> n;
    std::vector<int> xs(n);
    for (auto& x : xs)
    {
        std::cin >> x;
    }

    int64_t ans{ b };
    for (auto x : xs)
    {
        ans += std::min(x, a - 1);
    }
    std::cout << ans << '\n';
}
```

