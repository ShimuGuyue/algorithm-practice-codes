

```c++
void solve()
{
    std::string s;
    std::cin >> s;

    for (char& c : s)
    {
        c = tolower(c);
    }
    if (s == "yes")
        std::cout << "yes\n";
    else
        std::cout << "no\n";
}
```

