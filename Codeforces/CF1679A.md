

```cpp
void solve()
{
    int64_t n;
    std::cin >> n;
    if ((n & 1) || (n == 2))
    {
        std::cout << -1 << '\n';
        return;
    }

    int64_t min{ (n + 5) / 6 };
    int64_t max{ n / 4 };
    std::cout << min << ' ' << max << '\n';
}
```

