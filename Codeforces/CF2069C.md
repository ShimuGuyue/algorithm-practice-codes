

```c++
void solve()
{
    int n;
    std::cin >> n;
    std::vector<int> as(n);
    for (auto& a : as)
    {
        std::cin >> a;
    }

    int64_t ans{ 0 };
    int64_t dp1{ 0 }, dp2{ 0 }, dp3{ 0 };
    for (auto a : as)
    {
        if (a == 1)
            dp1 = (dp1 + 1) % mod;
        if (a == 2)
            dp2 = (dp2 * 2 + dp1) % mod;
        if (a == 3)
            ans = (ans + dp2) % mod;
    }
    std::cout << ans << '\n';
}
```

