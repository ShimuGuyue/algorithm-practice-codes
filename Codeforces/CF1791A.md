

```c++
void solve()
{
    static std::string s{ "codeforces" };
    char c;
    std::cin >> c;
    if (s.find(c) != std::string::npos)
        std::cout << "YES\n";
    else
        std::cout << "NO\n";
}
```

