

```c++
void solve()
{
    int n;
    std::cin >> n;
    std::string s;
    std::cin >> s;
    std::map<char, int> m;
    for (char c : s)
    {
        ++m[c];
    }
    int ans{ n - m[s.back()] };
    std::cout << ans << '\n';
}
```

