

```cpp
void solve()
{
    int n;
    std::cin >> n;
    for (int i{ 0 }; i < n; ++i)
    {
        int a;
        std::cin >> a;
        if (n == 1)
            std::cout << 1 << ' ';
        else
            std::cout << 2 << ' ';
    }
    std::cout << '\n';
}
```

