

```cpp
void solve()
{
    int n;
    std::cin >> n;
    std::string s;
    std::cin >> s;
    int count{ std::count(s.begin(), s.end(), '(') };
    if (count == n - count)
        std::cout << "Yes\n";
    else
        std::cout << "No\n";
}
```

