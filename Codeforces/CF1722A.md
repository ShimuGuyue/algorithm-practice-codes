

```c++
void solve()
{
    int n;
    std::cin >> n;
    std::string s;
    std::cin >> s;

    std::sort(s.begin(), s.end());

    if (s == "Timru")
        std::cout << "yes\n";
    else
        std::cout << "no\n";
}
```

