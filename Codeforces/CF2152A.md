

```c++
void solve()
{
    int n;
    std::cin >> n;
    std::set<int> digits;
    while (n--)
    {
        int a;
        std::cin >> a;
        digits.insert(a);
    }
    std::cout << digits.size() * 2 - 1 << '\n';
}a
```

