

```c++
void solve()
{
    int n;
    std::cin >> n;
    std::string s;
    std::cin >> s;

    s.push_back('W');

    bool b{ false }, r{ false };
    for (char c : s)
    {
        if (c == 'W')
        {
            if (b != r)
            {
                std::cout << "no\n";
                return;
            }
            b = r = false;
        }
        else if (c == 'B')
        {
            b = true;
        }
        else if (c == 'R')
        {
            r = true;
        }
    }
    std::cout << "yes\n";
}
```

