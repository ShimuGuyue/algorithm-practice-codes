

```cpp
void solve()
{
    int64_t x, y;
    std::cin >> x >> y;

    int64_t ans{ 0 };
    for (int64_t k{ 1 }; k * k <= x; ++k)
    {
        ans += std::max(std::min(y, (x - k) / k) - k, INT64_C(0));
    }
    std::cout << ans << '\n';
}
```

