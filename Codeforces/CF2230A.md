

```cpp
void solve()
{
    int64_t n, a, b;
    std::cin >> n >> a >> b;

    int64_t ans{
        std::min({
            n * a,
            (n + 2) / 3 * b,
            n / 3 * b + n % 3 * a
        })
    };

    std::cout << ans << '\n';
}
```

