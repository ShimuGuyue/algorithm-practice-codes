

```c++
void solve()
{
    int64_t b, c, d;
    std::cin >> b >> c >> d;

    int64_t ans{ 0 };
    for (int bit = 0; bit < 60; ++bit)
    {
        int bb{ int(b >> bit & INT64_C(1)) };
        int cc{ int(c >> bit & INT64_C(1)) };
        int dd{ int(d >> bit & INT64_C(1)) };
        int flag{ bb * 4 + cc * 2 + dd };
        switch (flag)
        {
        case 0:
            break;
        case 1:
            ans |= INT64_C(1) << bit;
            break;
        case 2:
            break;
        case 3:
            std::cout << -1 << '\n';
            return;
            break;
        case 4:
            std::cout << -1 << '\n';
            return;
            break;
        case 5:
            break;
        case 6:
            ans |= INT64_C(1) << bit;
            break;
        case 7:
            break;
        }
    }
    std::cout << ans << '\n';
}
```

