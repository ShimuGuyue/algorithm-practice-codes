

```c++
void solve()
{
    int a, b, n;
    std::cin >> a >> b >> n;
    int flag = a / b;
    if (flag >= n || a == b)
        std::cout << 1 << '\n';
    else
        std::cout << 2 << '\n';
}
```

