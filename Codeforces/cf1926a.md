

```cpp
void solve()
{
    std::string s;
    std::cin >> s;
    int count_a{ 0 };
    int count_b{ 0 };
    for (char c : s)
    {
        if (c == 'A')
            ++count_a;
        else
            ++count_b;
    }
    std::cout << (count_a > count_b ? "A\n" : "B\n");
}
```

