

```cpp
void solve()
{
    int n;
    std::cin >> n;
    std::vector<int> as(n + 1);
    for (int i{ 1 }; i <= n; ++i)
    {
        std::cin >> as[i];
    }

    if (n & 1)
    {
        std::cout << 4 << '\n';
        std::cout << 1 << ' ' << n << '\n';
        std::cout << 2 << ' ' << n << '\n';
        std::cout << 1 << ' ' << 2 << '\n';
        std::cout << 1 << ' ' << 2 << '\n';
    }
    else
    {
        std::cout << 2 << '\n';
        std::cout << 1 << ' ' << n << '\n';
        std::cout << 1 << ' ' << n << '\n';
    }
}
```

