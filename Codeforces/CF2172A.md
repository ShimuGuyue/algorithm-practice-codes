

```c++
void solve()
{
    std::array<int, 3> as;
    for (auto &a : as)
    {
        std::cin >> a;
    }
    std::sort(as.begin(), as.end());
    if (as.back() - as.front() >= 10)
        std::cout << "check again\n";
    else
        std::cout << "final " << as[1] << '\n';
}
```

