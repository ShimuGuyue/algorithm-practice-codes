

```cpp
void solve()
{
    int64_t a, b, c;
    cin >> a >> b >> c;
    if (llabs(a - b) > llabs(a + c - b))
        cout << llabs(a - b) << '\n';
    else
        cout << llabs(a + c - b) << '\n';
}
```

