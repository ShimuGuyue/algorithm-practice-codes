

```cpp
void solve()
{
    int64_t n, k;
    std::cin >> n >> k;

    int64_t count{ int64_t(n / (std::sqrt(2) * k)) };
    int64_t x{ count * k };
    int64_t y{ count * k + k };
    if (x * x + y * y <= n * n)
        std::cout << "Ashish\n";
    else
        std::cout << "Utkarsh\n";
}
```

