

```c++
void solve()
{
    int n;
    std::cin >> n;
    std::vector<std::pair<int, int>> as(n);
    for (int i = 0; i < n; ++i)
    {
        std::cin >> as[i].first;
    }

    {
        bool ok{true};
        auto bs = as;
        for (int i = 0; i < n; i += 2)
        {
            bs[i].second = 1;
        }
        std::sort(bs.begin(), bs.end());
        for (int i = 1; i < n; ++i)
        {
            if (bs[i].second == bs[i - 1].second)
                ok = false;
        }
        if (ok)
        {
            std::cout << "yes\n";
            return;
        }
    }
    {
        bool ok{true};
        auto bs = as;
        for (int i = 1; i < n; i += 2)
        {
            bs[i].second = 1;
        }
        std::sort(bs.begin(), bs.end());
        for (int i = 1; i < n; ++i)
        {
            if (bs[i].second == bs[i - 1].second)
                ok = false;
        }
        if (ok)
        {
            std::cout << "yes\n";
            return;
        }
    }

    std::cout << "no\n";
}
```

