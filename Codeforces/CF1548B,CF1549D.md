

```cpp
template<typename T, typename K = T>
class SparseTable
{
private:
    std::vector<std::vector<K>> table_;

    const std::vector<T>& v_;

public:
    SparseTable(const std::vector<T>& v) : v_(v)
    {
        const int n{ static_cast<int>(v.size()) };
        const int m{ static_cast<int>(std::log2(n)) + 1 };
        table_.assign(n, std::vector<K>(m));

        for (int i{ 0 }; i < n; ++i)
        {
            table_[i][0] = contribute(i);
        }
        for (int j{ 1 }; j < m; ++j)
        {
            const int len{ 1 << (j - 1) };
            for (int i{ 0 }; i + len * 2 - 1 < n; ++i)
            {
                table_[i][j] = merge(i, i + len, j - 1);
            }
        }
    }

public:
    K query(const int l, const int r) const
    {
        const int power{ static_cast<int>(log2(r - l + 1)) };
        const int len{ 1 << power };
        return merge(l, r - len + 1, power);
    }

private:
    K contribute(const int index) const
    {
        // TODO: 规定初始贡献值
        return v_[index];
    }

    K merge(const int i1, const int i2, const int j) const
    {
        // TODO: 规定贡献值合并结果
        return std::gcd(table_[i1][j], table_[i2][j]);
    }
};
```

```cpp
void solve()
{
    int n;
    std::cin >> n;
    std::vector<int64_t> as(n);
    for (auto& a : as)
    {
        std::cin >> a;
    }

    if (n == 1)
    {
        std::cout << 1 << '\n';
        return;
    }

    std::vector<int64_t> difs(n - 1);
    for (int i{ 0 }; i < n - 1; ++i)
    {
        difs[i] = std::llabs(as[i] - as[i + 1]);
    }

    SparseTable<int64_t> st(difs);

    int ans{ 1 };
    for (int i{ 0 }; i < n - 1; ++i)
    {
        if (difs[i] == 1)
            continue;
        int l{ i }, r{ n - 2 };
        while (l < r)
        {
            int mid{ (l + r + 1) / 2 };
            if (st.query(i, mid) != 1)
                l = mid;
            else
                r = mid - 1;
        }
        ans = std::max(ans, l - i + 1 + 1);
    }
    std::cout << ans << '\n';
}
```

