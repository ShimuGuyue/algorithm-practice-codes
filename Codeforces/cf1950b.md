

```cpp
void solve()
{
    int n;
    std::cin >> n;
    for (int i{ 1 }; i <= n; ++i)
    {
        for (int j{ 1 }; j <= n; ++j)
        {
            if ((i + j) % 2 == 0)
                std::cout << "##";
            else
                std::cout << "..";
        }
        std::cout << '\n';
        for (int j{ 1 }; j <= n; ++j)
        {
            if ((i + j) % 2 == 0)
                std::cout << "##";
            else
                std::cout << "..";
        }
        std::cout << '\n';
    }
}
```

