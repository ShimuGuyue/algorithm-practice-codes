

```c++
void solve()
{
    int64_t n;
    std::cin >> n;

    int64_t ans{ };
    for (int b = 0; b <= 60; ++b)
    {
        ans += n / (INT64_C(1) << b);
    }
    std::cout << ans << '\n';
}
```

